# 📝 LeetCode 26. Remove Duplicates from Sorted Array

## 1. 문제 설명
정렬된 정수 배열 `nums`에서 중복된 요소를 **In-place(제자리)**로 제거하여 각 요소가 한 번만 나타나도록 해야 합니다.
- 요소의 상대적 순서는 유지되어야 합니다.
- 새로운 배열을 할당하지 않고, **O(1) 추가 공간**만 사용하여 해결해야 합니다.
- 수정된 배열의 고유한 요소 개수 $k$를 반환합니다.

## 2. 해결 전략: 두 개의 포인터 (Two Pointers)
배열이 이미 **오름차순으로 정렬**되어 있다는 점을 활용합니다.

- **탐색 포인터 ($j$):** 1번 인덱스부터 끝까지 이동하며 숫자를 검사합니다.
- **기록 포인터 ($i$):** 새로운 고유 숫자가 발견될 때마다 값을 기록할 위치를 가리킵니다.
- **핵심 로직:** `nums[j] > nums[j-1]` 조건이 참이라면, 이전에 본 적 없는 새로운 숫자가 등장했다는 뜻이므로 기록 포인터 위치에 값을 쓰고 포인터를 이동시킵니다.


## 3. 코드 구현 (Python)

```python
from typing import List

class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        # 제약 조건상 nums.length >= 1 이지만, 빈 배열 예외 처리
        if not nums:
            return 0
        
        # i: 고유한 값이 저장될 인덱스 (0번은 항상 고유하므로 1번부터 시작)
        i = 1
        
        # j: 배열을 탐색하는 포인터
        for j in range(1, len(nums)):
            # 현재 값이 이전 값보다 크면 새로운 고유 숫자 발견
            if nums[j] > nums[j-1]:
                nums[i] = nums[j]
                i += 1
                
        return i

# 테스트 실행
if __name__ == "__main__":
    nums = [0, 0, 1, 2, 2, 3, 3, 3, 4, 4, 5, 5, 5]
    solution = Solution()
    k = solution.removeDuplicates(nums)
    
    print(f"Unique elements count (k): {k}")
    print(f"Modified nums: {nums[:k]}")
