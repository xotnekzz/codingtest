# 📝 LeetCode 66. Plus One (문제 해설)

## 1. 문제 개요
주어진 숫자 배열(예: `[1, 2, 3]`)을 하나의 큰 정수(`123`)로 보고, 여기에 **1을 더한 결과**를 다시 배열로 반환하는 문제입니다.

* **핵심 포인트:** 마지막 자릿수가 `9`일 때 발생하는 **올림(Carry)**을 어떻게 처리하느냐가 관건입니다.
    * `[1, 2, 9]` + 1 -> `[1, 3, 0]`
    * `[9, 9]` + 1 -> `[1, 0, 0]`

---

## 2. 작성하신 풀이 분석 (Greedy & Simulation)

작성하신 코드는 **초등학교 수학 시간**에 배우는 덧셈 방식(일의 자리부터 계산)을 그대로 구현한 것입니다.

### 💡 핵심 논리
1.  **뒤에서부터 탐색 (`range(len-1, -1, -1)`):** 가장 마지막 숫자(일의 자리)부터 확인합니다.
2.  **단순 덧셈:** 현재 자릿수에 1을 더합니다.
3.  **분기 처리:**
    * **10보다 작으면:** 올림이 발생하지 않으므로, 값을 갱신하고 **즉시 종료(`break`)**합니다.
    * **10이 되면 (즉, 원래 숫자가 9였으면):** 현재 자리를 `0`으로 만들고, 반복문을 계속 진행하여 앞자리에 1을 더해줍니다.
4.  **전체 자릿수 증가:** 만약 반복문이 끝났는데도 맨 앞자리가 `0`이라면(예: `99` -> `00`), 배열 맨 앞에 `1`을 붙여줍니다.

### 🔍 코드 리뷰

```python
class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        # 배열의 뒤에서부터 앞으로 순회
        for i in range(len(digits) - 1, -1, -1):
            # 1. 현재 자릿수가 9가 아니라면 1 더하고 바로 끝냄
            if digits[i] < 9:
                digits[i] += 1
                return digits  # 더 이상 볼 필요 없음
            
            # 2. 현재 자릿수가 9라면 0으로 바꾸고, 반복문 계속 (앞자리로 이동)
            digits[i] = 0
        
        # 3. 반복문을 다 돌았는데 리턴되지 않았다면? (예: 999 -> 000)
        # 맨 앞에 1을 추가해줌 (1000)
        return [1] + digits

```

---

## 3. 복잡도 분석

* **시간 복잡도:** 
* 배열을 한 번만 순회하므로 자릿수(N)에 비례합니다.
* 대부분의 경우(9가 아닌 경우) 끝까지 돌지 않고 중간에 `return` 하므로 매우 효율적입니다.


* **공간 복잡도:** 
* 주어진 `digits` 배열을 그대로 수정하여 사용하므로 추가적인 메모리 공간을 거의 쓰지 않습니다. (단, `99...9`인 경우에만 재할당 발생)



---

## 4. (참고) 파이썬만의 치트키 (Type Conversion)

알고리즘 인터뷰에서는 추천하지 않지만, 파이썬의 강력한 형변환 기능을 사용하면 한 줄로도 해결 가능합니다.

```python
class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        # 리스트 -> 문자열 -> 정수 변환 후 +1 -> 다시 리스트 변환
        return list(map(int, str(int(''.join(map(str, digits))) + 1)))

```

