# 📝 LeetCode 136. Single Number (심화 정리)

## 1. 문제 개요
비어 있지 않은 정수 배열에서, 딱 **하나의 요소만 한 번** 등장하고 나머지 모든 요소는 **두 번씩** 등장합니다. 그 한 번만 등장하는 요소를 찾아야 합니다.

- **제약 조건 1:** 선형 시간 복잡도 $O(N)$
- **제약 조건 2:** **상수 공간 복잡도 $O(1)$** (변수 몇 개만 써야 함, 배열/집합 생성 불가)

---

## 2. 풀이 방법 1: Set 활용 (작성하신 풀이)
"본 적 없는 숫자는 기억하고, 두 번째 등장하면 지워버린다"는 논리입니다.

### 💡 핵심 논리
1. `seen`이라는 집합(Set)을 만듭니다.
2. 숫자를 하나씩 확인하며:
   - 처음 본 숫자면? → `add` (기억함)
   - 이미 있는 숫자면? → `remove` (짝을 찾았으니 소거)
3. 짝이 없는 유일한 숫자는 지워지지 않고 `set`에 끝까지 남아있게 됩니다.

### 💻 코드
```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        seen = {nums[0]}
        for i in range(1, len(nums)):
            if nums[i] in seen:
                seen.remove(nums[i])  # 짝을 찾았으므로 제거
            else:
                seen.add(nums[i])     # 처음 보았으므로 추가
        return seen.pop()             # 남은 하나 반환

```

### 📊 복잡도 분석

* **시간 복잡도:**  (리스트를 한 번 순회)
* **공간 복잡도:** 
* ⚠️ **주의:** 최악의 경우 배열 크기에 비례하는 메모리를 사용합니다. (예: `[1, 2, 3, 4, 5, 1, 2, 3, 4]` 순서라면 중간에 set이 꽉 참)
* 따라서 문제의 **"Constant extra space"** 제약 조건을 만족하지 못합니다.



---

## 3. 풀이 방법 2: XOR 비트 연산 (최적화 풀이)

메모리를 거의 쓰지 않고() 풀기 위해 컴퓨터의 **비트 연산자(XOR, `^`)**의 성질을 활용합니다. 이 문제는 XOR 연산의 교과서적인 예제입니다.

### 💡 핵심 논리: XOR의 마법

XOR 연산(`^`)은 두 비트가 다르면 1, 같으면 0을 반환합니다. 이를 정수에 적용하면 다음과 같은 재미있는 성질이 있습니다.

1. **나 자신과 연산하면 사라짐:**  (똑같은 수 두 개는 0이 됨)
2. **0과 연산하면 그대로:** 
3. **순서는 상관없음 (교환법칙):** 

따라서, **배열의 모든 숫자를 다 XOR 해버리면**, 두 번 등장한 숫자들은 서로 만나서 **0으로 상쇄(Cancel out)**되어 사라지고, **오직 한 번 등장한 숫자만 남게 됩니다.**

### 📝 예시 흐름 (`[4, 1, 2, 1, 2]`)


순서를 바꿔서 짝끼리 붙여보면,


### 💻 코드

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        result = 0
        for num in nums:
            result ^= num  # 모든 숫자를 XOR 연산 누적
        return result

```

### ⚖️ 비교 및 결론

| 구분 | 풀이 1 (Set 활용) | 풀이 2 (XOR 활용) |
| --- | --- | --- |
| **논리 직관성** | ⭐⭐⭐⭐⭐ (이해가 매우 쉬움) | ⭐⭐⭐ (XOR 성질을 알아야 함) |
| **시간 복잡도** |  |  |
| **공간 복잡도** | **** (탈락 ⚠️) | **** (통과 ✅) |

> **💡 결론:**
> * 작성하신 코드는 일반적인 상황에서는 훌륭한 정답입니다.
> * 하지만 **"메모리 제한이 매우 빡빡한 임베디드 환경"**이나 **"시스템 최적화"**를 묻는 면접에서는 **XOR 풀이**를 요구하는 경우가 많으니 꼭 익혀두시는 것이 좋습니다.
> 
> 

