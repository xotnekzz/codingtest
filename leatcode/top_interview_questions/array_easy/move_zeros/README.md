# 📝 LeetCode 283. Move Zeroes (심화 정리)

## 1. 문제 개요
정수 배열 `nums`가 주어졌을 때, **0들을 모두 배열의 맨 끝으로 이동**시키는 문제입니다.

### ⚠️ 핵심 제약 조건
1. **순서 유지 (Relative Order):** 0이 아닌 숫자들의 상대적인 순서는 변경되어서는 안 됩니다.
2. **In-Place (제자리 연산):** 배열의 복사본을 만들지 않고, 주어진 배열 내에서 조작해야 합니다. (공간 복잡도 $O(1)$)

---

## 2. 풀이 방법: Two Pointers (투 포인터)

배열을 한 번만 순회하면서 해결하기 위해 두 개의 포인터(`left`, `right`)를 사용하는 방식입니다. 이 방식은 **"0이 아닌 숫자를 앞으로 밀어 넣는다(Swap)"**는 개념을 사용합니다.

### 💡 핵심 논리
- **`right` 포인터**: 배열 전체를 탐색하는 스카우트 역할을 합니다. (현재 검사 중인 요소)
- **`left` 포인터**: 0이 아닌 숫자가 채워질 **'다음 위치'**를 가리킵니다.

**동작 원리:**
1. `right`가 가리키는 숫자가 **0이 아니라면**, 현재 `left` 위치의 숫자와 스왑(Swap)합니다.
2. 스왑 후에는 `left` 위치가 채워졌으므로 `left`를 한 칸 전진시킵니다.
3. `right`가 가리키는 숫자가 **0이라면**, 아무것도 하지 않고 `right`만 전진합니다. (결과적으로 0은 뒤로 밀려나는 효과)

---

## 3. 코드 분석

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        # left: 0이 아닌 숫자를 배치할 인덱스 포인터
        left = 0 
        
        # right: 배열 전체를 순회하는 인덱스
        for right in range(len(nums)):
            
            # 현재 숫자가 0이 아닌 경우에만 처리
            if nums[right] != 0:
                
                # [최적화] 자기 자신과의 스왑(left == right)을 피하려면 조건 추가 가능
                # if left != right:
                
                # 현재 숫자(right)를 앞으로(left) 당겨옴 (Swap)
                nums[left], nums[right] = nums[right], nums[left]
                
                # 다음 0이 아닌 숫자가 올 위치로 이동
                left += 1

```

---

## 4. 복잡도 분석

### ⏳ 시간 복잡도 (Time Complexity)

* ****: 배열의 길이 만큼 `right` 포인터가 한 번 순회하므로 선형 시간이 소요됩니다.

### 💾 공간 복잡도 (Space Complexity)

* ****: 추가적인 배열을 생성하지 않고 `left`, `right` 변수만 사용하여 제자리에서(In-place) 연산합니다.

---

## 5. Follow up: 연산 최소화 (Minimize Operations)

문제의 Follow up 질문인 "총 연산 횟수를 최소화할 수 있는가?"에 대해, 위 코드는 매우 효율적입니다.

* **방식 비교:**
1. **덮어쓰기 후 채우기:** 0이 아닌 수를 앞으로 싹 덮어쓰고, 남은 뒤쪽을 0으로 채우는 방식은 0이 많을 때 쓰기(Write) 연산이 더 많이 발생할 수 있습니다.
2. **스왑(Swap) 방식 (현재 코드):** 0이 아닌 요소만 딱 필요한 위치로 스왑하므로, 특히 배열 앞부분에 0이 없는 경우 불필요한 쓰기 연산을 줄이면서 한 번의 패스(One-pass)로 완료됩니다.
