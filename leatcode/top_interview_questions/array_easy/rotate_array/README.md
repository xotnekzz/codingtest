# 📝 LeetCode 189. Rotate Array (심화 정리)

## 1. 문제 개요
주어진 정수 배열 `nums`를 오른쪽으로 `k`번 회전시키는 문제입니다.
- **In-place** 방식으로 배열을 수정해야 하며, 별도의 반환 값은 없습니다.
- `k`가 배열의 길이보다 클 수 있음에 유의해야 합니다.

---

## 2. 풀이 방법 1: Slicing (슬라이싱 활용)
사용자께서 작성하신 코드로, 파이썬의 **Slicing** 기능을 활용해 배열의 앞뒤를 잘라 붙이는 가장 직관적인 방법입니다.

### 💡 핵심 논리
- **나머지 연산**: `k = k % n`을 통해 불필요한 회전을 방지합니다. (배열 길이가 5일 때 6번 회전하는 것은 1번 회전하는 것과 같습니다.)
- **리스트 전체 업데이트**: `nums[:]`를 사용하여 리스트 객체 자체를 바꾸지 않고 내부의 요소들만 한꺼번에 교체합니다.

### 💻 코드
```python
class Solution:
    def rotate(self, nums: list[int], k: int) -> None:
        n = len(nums)
        k = k % n
        # 뒤에서 k개 + 앞에서 n-k개
        nums[:] = nums[n-k:] + nums[:n-k]

- 시간 복잡도: $O(n) (슬라이싱 및 이어붙이기에 소요)
- 공간 복잡도: $O(n) (슬라이싱 결과물이 일시적으로 새로운 메모리를 점유)
```


## 3. 풀이 방법 2: Three Reverses (공간 복잡도 O(1))
배열을 세 번 뒤집는 논리를 사용하여, 추가적인 리스트 생성 없이 원본 리스트 안에서 자리를 바꿉니다.

### 💡 핵심 논리
오른쪽으로 $k$번 회전한다는 것은 결국 **뒤의 $k$개가 앞으로 오고, 앞의 $n-k$개가 뒤로 밀리는 것**입니다. 이를 '뒤집기'를 통해 구현할 수 있습니다.


1. **전체 뒤집기**: 모든 원소의 순서를 거꾸로 바꿉니다.
2. **앞부분 뒤집기**: 앞에서부터 $k$개까지만 다시 뒤집어 원래 순서로 복구합니다.
3. **뒷부분 뒤집기**: 나머지 $k$번째부터 끝까지를 다시 뒤집어 원래 순서로 복구합니다.

### 💻 코드
```python
class Solution:
    def rotate(self, nums: list[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        k %= n  # k가 n보다 큰 경우를 대비한 나머지 연산
        
        # 특정 구간을 뒤집는 내부 함수
        def reverse(start: int, end: int) -> None:
            while start < end:
                nums[start], nums[end] = nums[end], nums[start]
                start += 1
                end -= 1
        
        # 1. 전체를 뒤집음: [7, 6, 5, 4, 3, 2, 1]
        reverse(0, n - 1)
        
        # 2. 앞 k개를 뒤집음: [5, 6, 7, 4, 3, 2, 1]
        reverse(0, k - 1)
        
        # 3. 뒤머지 부분을 뒤집음: [5, 6, 7, 1, 2, 3, 4]
        reverse(k, n - 1)
```

- 시간 복잡도: $O(n) (모든 요소를 최대 2번씩 방문)
- 공간 복잡도: $O(1) (포인터 변수 외에 추가 메모리 없음)

